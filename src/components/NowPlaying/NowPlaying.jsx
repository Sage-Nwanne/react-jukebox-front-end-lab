import React from 'react';

const NowPlaying = () => {
  return (
    <div>
      <h2>Now Playing</h2>
      {/* Display the currently playing track information */}
    </div>
  );
};

export default NowPlaying;